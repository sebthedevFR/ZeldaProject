{% extends "ZeldaBundle::layout.html.twig" %}
{% block title %}{{ uneArme.libelle }}{% endblock %}
{% block pageContent %}
        <!-- Portfolio Item Heading -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">{{ uneArme.libelle }}
                    <small>{{ uneArme.numeroIg }}</small>
                </h1>
            </div>
        </div>

        <!-- Portfolio Item Row -->
        <div class="row">
            <div class="col-md-6" align="center" >
                <img class="img-responsive" src="{{ uneArme.vignetteUrl }}" alt="">
            </div>
            <div class="col-md-4">
                <h3>Caractéristiques </h3>
                <p align="justify"><i>{{ uneArme.description }}</i></p>
                {% if uneArme.getLesLieux() is empty %}
                    <h3 align="center">Principaux lieux de disponibilité</h3>
                    <ul>
                        <li>Inconnu</li>
                    </ul>
                {% else %}
                    <h3 align="center">Principaux lieux de disponibilité</h3>
                    <ul>
                        {% for lieu in uneArme.getLesLieux() %}
                            <li>{{ lieu.getLibelle() }}</li>
                        {% endfor %}
                    </ul>
                {% endif %}
                <h3 align="center">Efficacité</h3>
                <ul>
                    <li>{{ uneArme.efficacite }}</li>
                </ul>
            </div>
            <div class="col-md-2"></div>
        </div>
        <!-- /.row -->

        {% if uneArme.getLesImages() is empty %}
        {% else %}
        <div class="row">
            <div class="col-lg-12">
                <h3 class="page-header">Images</h3>
            </div>
            {% for images in uneArme.getLesImages() %}
            <div class="col-sm-3 col-xs-6">
                <a href="{{ images.url }}">
                    <img class="img-responsive portfolio-item" src="{{ images.url }}" alt="{{ images.alt }}">
                </a>
            </div>
            <!-- Modal -->
            {% endfor %}
        </div>
        {% endif %}

        <div class="row">
            <div class="col-lg-12">
                <h3 class="page-header">Autres épées à une main.</h3>
            </div>
            {% for uneArmes in lesArmes %}
            <div class="col-sm-1 col-xs-2">
                <a href="{{  path('zelda_item', {'id': uneArmes.id}) }}" >
                    <img class="img-responsive" src="{{ uneArmes.vignetteUrl }}" alt="" width="100" height="100">
                </a>
                <h4>
                    <a href="{{ path('zelda_item', {'id': uneArmes.id}) }}">{{ uneArmes.libelle }}</a>
                </h4>
            </div>
            {% endfor %}
        </div>
        <!-- /.row -->

{% endblock %}
